<h1>New Location</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.location_path(@conn, :create, @event.id)) %>

<span><%= link "Back", to: Routes.location_path(@conn, :index, @event.id) %></span>
