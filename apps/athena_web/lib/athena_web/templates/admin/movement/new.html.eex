<h1>New Movement</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_movement_path(@conn, :create, @item.id)) %>

<span><%= link "Back", to: Routes.admin_movement_path(@conn, :index, @item.id) %></span>
