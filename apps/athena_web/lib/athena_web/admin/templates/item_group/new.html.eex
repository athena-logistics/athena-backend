<h1>New Item group</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_item_group_path(@conn, :create, @event.id)) %>

<span><%= link "Back", to: Routes.admin_item_group_path(@conn, :index, @event.id) %></span>
