<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8"/>
		<meta http-equiv="X-UA-Compatible" content="IE=edge"/>
		<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		<title><%= assigns[:page_title] || "Athena · Phoenix Framework" %></title>
		<link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/style.css") %>"/>
		<link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700,800" rel="stylesheet">
		<%= csrf_meta_tag() %>
	</head>
	<body class="has_nav">
		<input type="checkbox" id="toggle-nav" hidden />
		<nav>
			<header>
				<span><%= @event.name %></span>
				<label for="toggle-nav">
					<span></span>
				</label>
			</header>
			<main>
				<ul>
					<li>
						<%= link "Logistik Übersicht", to: Routes.frontend_logistics_live_url(@conn, AthenaWeb.Frontend.LogisticsLive, @event.id) %>
					</li>
					<li>
						<%= link "Eingang Artikel", to: Routes.frontend_logistics_movement_url(@conn, :supply_new, @event.id) %>
					</li>
					<li>
						<%= link "Verschiebung Artikel", to: Routes.frontend_logistics_movement_url(@conn, :relocate_new, @event.id) %>
					</li>
					<li class="divider"></li>
					<%= for %{id: location_id, name: location_name} <- @locations do %>
						<li>
							<%= link location_name, to: Routes.frontend_logistics_location_url(@conn, :show, location_id) %>
						</li>
					<% end %>
				</ul>
			</main>
		</nav>
		<main role="main" class="container">
			<p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
			<p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
			<%= render @view_module, @view_template, assigns %>
		</main>
		<script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
	</body>
</html>
